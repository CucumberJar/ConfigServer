server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          username: CucumberJar
          password: 123GitMMM
          uri: https://github.com/CucumberJar/SCConfig
          default-label: main